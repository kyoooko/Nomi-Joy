<header class="admin-header">
  <%= render 'admin/logging_in_header' %>
</header>
<main class="row">
  <div class="sidebar admin-sidebar">
    <%= render 'admin/logging_in_slidebar' %>
  </div>  
  <div class="body">
    <div class="heading">
      <h5 class="heading__create-event admin">ノミカイをつくる</h5>
      <h1 class="heading__en admin">Step4</h1>
    </div>
    <div class="content">
      <div class="content__block--step-bar admin-table">
        <h3>会費設定</h3>
        <%# ステップバー（Bootstrap・見た目のみ） %>
        <div class="create-nomikai-stepbar">
          <ul class="bootstrapWizard form-wizard">
            <li class="" data-target="#step1"><span class="step">1</span></li>
            <li data-target="#step2" class=""><span class="step">2</span></li>
            <li data-target="#step3" class=""><span class="step">3</span></li>
            <li data-target="#step3" class="active"><span class="step">4</span></li>
            <li data-target="#step3" class=""><span class="step">5</span></li>
          </ul>
          <div class="clearfix"></div>
      </div> 
      <div class="content__block--step4 content__block">
        <p class="text-muted">※ 後ほど設定できます<br>※ 半角数字で入力</p>
        <%= form_with  model: @event, url: admin_confirm_path,  local: true do |f| %>
          <table class="table table-borderless">
            <tbody>
              <tr>
                <td class="nomal-font font-sm">
                  <%# カンジ %>
                  <span class="fee-name nomal-font admin"><%= User.find(@event.user_id).name %>（カンジ）</span>&emsp;<%= text_field_tag "admin_fee","",class:"fee-form" %>円<br />
                  <%# カンジ以外の参加メンバー %>
                  <% @event_user_ids.each do |event_user_id| -%>
                    <span class="fee-name nomal-font admin"><%= User.find(event_user_id).name %></span>&emsp;<%= text_field_tag "fees[]","",class:"fee-form" %>円<br>
                  <% end -%>
                </td>
              </tr>
            </tbody>
          </table>
          <%= f.hidden_field :name %>
          <%= f.hidden_field :date %>
          <%= f.hidden_field :begin_time %>
          <%= f.hidden_field :finish_time %>
          <%= f.hidden_field :restaurant_id %>
          <%= f.hidden_field :memo %>
          <div class="actions"> 
            <%= f.submit "戻る", name: :back, class: "btn admin-white-btn" %>
            <%= f.submit "次へ", class: "btn blue-btn" %>
          </div> 
        <% end %>
      </div>
    </div>
    <%= render "layouts/admin_footer" %>
  </div>
</main>



















