# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
# default: &default
#   adapter: sqlite3
#   pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#   timeout: 5000

# development:
#   <<: *default
#   database: db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: db/test.sqlite3
## ******************************************
# development:
#   adapter: mysql2
#   encoding: utf8
#   database: blog_development
#   pool: 5
#   username: root
#   password:
#   socket: /tmp/mysql.sock

## ******************************************

# default: &default
#   adapter: mysql2
#   encoding: utf8
#   pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
#   timeout: 5000
#   url: <%= ENV['DATABASE_URL'] %>

# development:
#   <<: *default
#   database: db_development

# test:
#   <<: *default
#   database: db_test

# production:
#   <<: *default
#   database: db_production
# ******************************************
default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: root
  password: 
  # host: localhost


development:
  <<: *default
  database: nomijoy_development #DBの名前（任意で付けられる）

test:
  <<: *default
  database: nomijoy_test #DBの名前（任意で付けられる）
 # ****************************************** 

production:
  <<: *default
  database: <%= ENV['DB_DATABASE'] %>
  adapter: mysql2
  encoding: utf8mb4
  charset: utf8mb4
  collation: utf8mb4_general_ci
  host: <%= ENV['DB_HOST'] %>
  username: <%= ENV['DB_USERNAME'] %>
  password: <%= ENV['DB_PASSWORD'] %>